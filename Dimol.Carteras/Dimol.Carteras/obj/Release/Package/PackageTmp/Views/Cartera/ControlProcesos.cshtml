@{
    ViewBag.Title = "Control de Procesos";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Control de Procesos</h2>

<script src="~/Scripts/jquery.validate.min.js"></script>
<script src="~/Scripts/jquery.validate.unobtrusive.min.js"></script>
@using Mvc.HtmlHelpers
<div class="ui-widget ui-widget-content ui-corner-all" style="outline: 0px none;">
    <div class="ui-widget-header ui-corner-all">
        <span id="ui-dialog-title-dialog" class="ui-dialog-title">Panel Control de Procesos</span>

    </div>
    <div style="height: auto; min-height: 300px; width: auto; margin: 10px;border:none" class="ui-widget-content">
        @*@using (Html.BeginForm(null, null, FormMethod.Post, new { id = "frmBuscarDeudor" }))
        {
            @Html.ValidationSummary(true)

            <fieldset>
                <legend>BuscarDeudor</legend>

                <div class="tabla">
                    <div class="fila">
                        <div class="col" style="width:160px">@Html.Label("Rut")</div>
                        <div class="col"> @Html.TextBox("Rut", null, new { @class = "textboxClassRut" })</div>
                    </div>
                    <div class="fila">
                        <div class="col" style="width:160px">@Html.Label("Nombre")</div>
                        <div class="col">
                            @Html.TextBox("Nombre", null, new { @class = "textboxClass" })
                    </div>
                </div>

                <div class="fila">
                    <div class="col" style="float:right"><input type="button" value="Buscar" onclick="fnBuscarSubcarteras();" /></div>
                </div>

            </div>
        </fieldset>
        }*@
        <br />
        @(Html.jqGrid("gridProceso")

    .addColumn(new Column("IdProceso").setLabel("Id")
        .setHidden(true)
        .setWidth(50))
    .addColumn(new Column("Nombre").setLabel("Nombre")
        .setSortable(true)
        .setWidth(110))
    .addColumn(new Column("Servidor").setLabel("Servidor")
        .setSortable(true)
        .setWidth(50))
    .addColumn(new Column("Running").setLabel("Estado")
        .setSortable(true).setCustomFormatter("fnInterruptorProceso").setAlign(Align.center)
        .setWidth(50))
    
                // settings
    .setCaption("Procesos")
    .setRequestType(RequestType.get)
    .setUrl("~/Cartera/GetProcesos")
    .setAltRows(true)
    .setAltClass("altGridRows")
    .setAutoWidth(true)
    .setRowNum(10)
    .setRowList(new int[] { 10, 30, 50 })
    .setViewRecords(true)
    .setSortName("Nombre")
    .setSortOrder(SortOrder.asc)
    .setPager("pagerSubCartera")
    .setPagerPos(PagerPos.center)
    .setPgButtons(true)
    .Render()

        )
        <br />
    </div>
</div>
